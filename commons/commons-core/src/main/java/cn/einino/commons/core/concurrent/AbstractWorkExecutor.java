package cn.einino.commons.core.concurrent;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public abstract class AbstractWorkExecutor {

	protected final Logger logger = LoggerFactory.getLogger(getClass());
}
